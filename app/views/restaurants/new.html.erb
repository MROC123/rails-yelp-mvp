<h1>New Restaurant</h1>

<%= simple_form_for [@restaurant] do |f| %>
  <% f.input :name %>
  <% f.input :address %>
  <% f.input :phone_number %>
  <%= f.select :category, collection: Restaurant::TYPE %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
